import 'dart:ui';

import 'package:flutter/material.dart';

/// Global variables
class Globals{
  static const String appName = "Wishlist'r";
  static const Color themeColor = Color.fromRGBO(99, 235, 185, 1);
  static const Color accentThemeColor = Colors.teal;
  static const TextStyle bigTextStyle = TextStyle(fontSize: 24);

}